#include <iostream>
#include "image.h"

int main() {
    Image im = load_image("data/dogsmall.jpg");
    // float result = im.pixel_nearest(0.02, 0.3, 1);
    // std::cout << result  << std::endl;

    return 0;
 
}